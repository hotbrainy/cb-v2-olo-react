{/* ref: https://www.youtube.com/watch?v=lWk5SntifCU */}

import { ArgsTable } from "@storybook/addon-docs";
import { Canvas } from "@storybook/addon-docs";
import { Meta } from "@storybook/addon-docs";
import { Story } from "@storybook/addon-docs";
import { withDesign } from "storybook-addon-designs";
import StandaloneButton from "./";
import paletteColors from "src/theme/paletteColors";

export const defaultArgs = {
  buttonText: "Order Now",
  buttonColor: paletteColors.red,
  buttonHoverColor: paletteColors.red,
  buttonTextColor: paletteColors.white,
  font: "Cera",
};

<Meta
  title="Chicken Treat/Buttons/StandaloneButton"
  decorators={[withDesign]}
  component={StandaloneButton}
  args={defaultArgs}
/>

export const Template = (args) => <StandaloneButton {...args} />;
export const Default = Template.bind({});

# StandaloneButton

Let's define a story for our `StandaloneButton` component:

<Canvas>
  <Story
    name="Default"
    args={defaultArgs}
    parameters={{
      design: {
        type: "figma",
        url: "https://www.figma.com/file/1HThAFViVYomDz4ElEP1ep/Craveable%3A-Chicken-Treat-Website?node-id=927%3A24105",
      },
    }}
  >
    {Default}
  </Story>
</Canvas>

<ArgsTable of={StandaloneButton} />
