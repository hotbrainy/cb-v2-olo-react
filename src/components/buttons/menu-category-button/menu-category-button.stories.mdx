{/* ref: https://www.youtube.com/watch?v=lWk5SntifCU */}

import {ArgsTable}        from '@storybook/addon-docs';
import {Canvas}           from '@storybook/addon-docs';
import {Meta}             from '@storybook/addon-docs';
import {Story}            from '@storybook/addon-docs';
import {withDesign}       from 'storybook-addon-designs';
import MenuCategoryButton from './';

import mockImage from './mock/your-favourites.svg';

export const defaultArgs = {
    title    : 'Your Favourites',
    imageUrl : mockImage,
};

<Meta
    title="Chicken Treat/Products/MenuCategoryButton"
    decorators={[withDesign]}
    component={MenuCategoryButton}
    argTypes={{
        onClick : {action : 'clicked'}
    }}
    args={defaultArgs}
/>

export const Template = (args) => <MenuCategoryButton {...args} />;
export const Default  = Template.bind({});

# MenuCategoryButton

Let's define a story for our `MenuCategoryButton` component:

<Canvas>
    <Story
        name="Default"
        args={defaultArgs}
        parameters={{
            design : {
                type : 'figma',
                url  : 'https://www.figma.com/file/1HThAFViVYomDz4ElEP1ep/Craveable%3A-Chicken-Treat-Website?node-id=2%3A362'
            }
        }}
    >
        {Default}
    </Story>
</Canvas>

<ArgsTable of={MenuCategoryButton}/>
